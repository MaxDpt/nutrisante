<div class="info-content">
<div class="info-button">
    <button class="update" id="info_update_service" value={{service.id}}>Modifier</button>
    <button id="info_delete" value={{service.id}}>Supprimer</button>
</div>

<table class="table table-hover">
  <tbody>
    <tr>
      <td>{{service.name}} </td>
    </tr>
      <tr>
      <td>{{service.description}} </td>
    </tr>
  </tbody>
</table>
</div>

{% include "partials/_confirm.html.twig" %}