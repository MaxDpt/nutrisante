<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Nom</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
{% if not services is same as([]) %}
{% for services in services %}
    <tr>
      <th scope="row">{{services.name}}</th>
      <td id="table-btn"><button id="serviceClass" value={{services.id}}>consulter</button></td>
    </tr>
{% endfor %}
{% else %}
    <tr>
      <th> Aucun services enregistré. </th>
      <td></td>
      <td></td>
      <td></td>
    </tr> 
{% endif %} 
  </tbody>
</table>