{% if not services is same as([]) %}
{% for service in services %}
    <div class="service_content">
        <div class="service_header"><h2>{{service.name}}</h2></div>
        <div class="service_description">{{service.description}}</div>
    </div>
{% endfor %}
{% else %}
      <p> Aucun service enregistré. </p>
{% endif %} 